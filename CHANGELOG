Alfa gem changelog
+ = new feature, - = removed feature, b = bugfix, s = security issue, p = perfomance issue, * = other, ! = important, . = minor

=== HEAD

+ Escaping variables in templates by default (vsyrovat)

+ Using Sequel as main database engine (vsyrovat)

+ Router.mount with tests (vsyrovat)

=== 0.0.1.pre

* Basic functionality
